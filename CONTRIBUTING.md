# Contributing

Contributions are welcome when they improve the public contracts without
coupling them to DreamNet's private engine.

1. Open an issue describing the contract or interoperability problem.
2. Keep runtime dependencies at zero unless a dependency is essential.
3. Add or update tests for every behavioral change.
4. Run `npm run verify`.
5. Submit a focused pull request with compatibility notes.

Breaking schema changes require a new schema version.
